# Campus Apps — InternNow & BunkBROS

Two React prototypes in one project, switchable from the top bar.

## Run it

```bash
npm install
npm run dev
```

Then open the local URL Vite prints (usually http://localhost:5173).

## Build for production

```bash
npm run build
npm run preview
```
